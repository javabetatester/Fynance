# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=fynance
DB_PASSWORD=fynance123
DB_NAME=fynance
DB_SSL_MODE=disable
DB_TIMEZONE=UTC
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# JWT Configuration
# IMPORTANTE: Gere uma chave secreta forte (mínimo 32 caracteres) para produção
# Você pode usar: openssl rand -base64 32
JWT_SECRET_KEY=sua-chave-super-mega-secreta-aqui
JWT_ISSUER=fynance_api
JWT_EXPIRES_IN=24h

# Application Configuration
APP_ENV=development
LOG_LEVEL=info

# Server Configuration
SERVER_PORT=8080
SERVER_READ_TIMEOUT=15s
SERVER_WRITE_TIMEOUT=15s
SERVER_IDLE_TIMEOUT=60s
